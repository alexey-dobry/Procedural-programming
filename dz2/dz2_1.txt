#include <iostream>
#include <Windows.h>
#include <math.h>
using namespace std;

void dz2_1()
{
    float R, r, h,pi;
    pi = 3, 1415;
    cout << "Радиус нижнего основания: ";
    cin >> R;
    cout << "Радиус верхнего основания: ";
    cin >> r;
    cout << "Высота усеченного конуса: ";
    cin >> h;
    cout << "V = " << (0,3 * pi * h * (R * 2 + R * r + r * 2)) << endl;
    cout << "S = " << (pi * (R * 2 + (R + 1) + r * 2)) << endl;
}

int main()
{
    setlocale(0, "");
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);

    dz2_1(); 

}