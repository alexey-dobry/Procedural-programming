#include <iostream>
#include <Windows.h>
#include <math.h>
#include <fstream>
#include <string>
#include <algorithm>

void dz_1()
{
    ofstream file("41.txt");
    int i;
    float answer = 0;
    string str,s;
    float numb;
    for (i = 0; i < 10; i++) {
        cin >> numb;
        s = s + to_string(numb);
        s += "\n";
    }
    file << s;
    file.close();

    ifstream File;
    File.open("41.txt");
    if (!File.is_open()) {
        cout << "File is closed";
    }
    else {
        while (getline(File, str)) {
            answer += stoi(str);
        }
    }
    cout << answer;
}

int main()
{
    dz_1();
    return 0;
}